# URL of the deployed FraudShield FastAPI inference service (no trailing slash).
# Never hardcode localhost in production components — this is the only source of the API URL.
NEXT_PUBLIC_API_URL=https://your-api-domain.com
